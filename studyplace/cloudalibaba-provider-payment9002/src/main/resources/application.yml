server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848 #配置Nacos地址
        server-addr: 192.168.189.129:1111  #虚拟机上的远程端口

management:
  endpoints:
    web:
      exposure:
        include: '*'